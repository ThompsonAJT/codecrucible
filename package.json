{
  "name": "codecrucible",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "test:judge": "npm --prefix server run test:judge",
    "pull-images": "docker pull python:3.12-slim && docker pull node:20-slim && docker pull eclipse-temurin:21-jdk && docker pull gcc:13"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
